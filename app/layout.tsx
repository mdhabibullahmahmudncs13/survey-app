import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'NITER Computer Club - Robotics Workshop Registration',
  description: 'Register for the NITER Computer Club Robotics Workshop. Learn Arduino, AI, sensors, and cutting-edge robotics technology.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}